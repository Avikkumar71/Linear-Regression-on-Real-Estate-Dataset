# -*- coding: utf-8 -*-
"""Untitled5.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/10Z11cr2dsyZX39R49bEnVCa_V_Ql-xnP
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

from sklearn.datasets import fetch_california_housing

dataset = pd.read_csv('/content/Real estate.csv')

dataset

dataset.describe()

X = dataset[['X1 transaction date','X2 house age','X3 distance to the nearest MRT station','X4 number of convenience stores','X5 latitude','X6 longitude','Y house price of unit area']]

Y = dataset['No']

X

Y

from sklearn.model_selection import train_test_split

X_train, X_test, Y_train, Y_test = train_test_split(X, Y, test_size=0.2, random_state=75)

X_train

X_test

Y_train

Y_test

from sklearn.linear_model import LinearRegression

lm = LinearRegression()

lm.fit(X_train,Y_train)

predictions = lm.predict(X_test)

predictions

Y_test

from sklearn import metrics

print('MAE: ',metrics.mean_absolute_error(Y_test, predictions))

print('MSE: ',metrics.mean_squared_error(Y_test, predictions))

print('RMSE: ',np.sqrt(metrics.mean_squared_error(Y_test, predictions)))

from sklearn.preprocessing import MinMaxScaler

scaler = MinMaxScaler()

df = dataset.drop(columns = [ ])

df1 = pd.DataFrame(scaler.fit_transform(df), columns=df.columns)

df1

x1=df1[['X1 transaction date','X2 house age','X3 distance to the nearest MRT station','X4 number of convenience stores','X5 latitude','X6 longitude','Y house price of unit area']]

y1 = df1['No']

x1

y1

x1_train, x1_test, y1_train, y1_test = train_test_split(x1, y1, test_size=0.2, random_state=75)

x1_train

y1_train

x1_test

y1_test

lm.fit(x1_train,y1_train)

pred = lm.predict(x1_test)

pred

print('MAE: ',metrics.mean_absolute_error(y1_test, pred))

print('MSE: ',metrics.mean_squared_error(y1_test, pred))

print('RMSE: ',np.sqrt(metrics.mean_squared_error(y1_test, pred)))

